# ImmoGuinee Mobile App Environment Configuration
# Copy this file to .env and fill in the values

# API Configuration
EXPO_PUBLIC_API_URL=https://immoguinee.com/api
EXPO_PUBLIC_API_HOST=immoguinee.com

# App Version
EXPO_PUBLIC_APP_VERSION=1.0.0

# WebSocket (Laravel Reverb) Configuration
# Get the key from production: docker exec $(docker ps -q -f name=immog_php | head -1) cat /run/secrets/reverb_app_key
EXPO_PUBLIC_REVERB_KEY=your-reverb-app-key-here
EXPO_PUBLIC_REVERB_HOST=immoguinee.com
EXPO_PUBLIC_REVERB_PORT=443
EXPO_PUBLIC_REVERB_SCHEME=https

# SSL Certificate Pinning (SHA-256 Base64 encoded public key hashes)
# Get the pin with: openssl s_client -connect immoguinee.com:443 2>/dev/null | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64
# Primary pin (current Let's Encrypt certificate)
EXPO_PUBLIC_SSL_PIN_PRIMARY=LOv+QdFaqnOuyIXGtn5hPngBXKLwCylu5Py+5oBPRVo=

# Backup pin (for certificate renewal - update before expiry)
EXPO_PUBLIC_SSL_PIN_BACKUP=
